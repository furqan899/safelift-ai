from django.apps import AppConfig


class EscalationsConfig(AppConfig):
    """Configuration for escalations app."""
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.escalations"
    verbose_name = "Escalations"